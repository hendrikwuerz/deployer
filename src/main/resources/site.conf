server {
  listen 0.0.0.0:80;
  server_name {full-domain};

  location / {
    proxy_pass http://{target-wf}:{port}/;
  }
  access_log /var/log/nginx/default_access.log;
  error_log /var/log/nginx/default_error.log;

  client_max_body_size 1000M;
}

